apiVersion: v1
kind: Secret
metadata:
  name: ror-firewall-ssi-{{ .Values.settings.priority }}-{{ .Values.settings.infrastructure }}-secrets
  namespace: ssi
type: Opaque
stringData:
  secrets.yaml: |
    ---
    NAM_TOKEN: {{ .Values.credentials.namToken | quote }}
    SPLUNK_TOKEN: {{ .Values.credentials.splunkToken | quote }}
