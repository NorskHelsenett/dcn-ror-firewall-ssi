schedule: "*/5 * * * *"

limits:
  memory:
    min: "384Mi"
    max: "1024Mi"
  cpu:
    min: "200m"
    max: "800m"

settings:
  environment: development
  infrastructure: test
  priority: "{{ .Values.settings.priority }}"
  interval: 300
  continuousMode: false

integration:
  nam:
    url: "https://qa.dcn.nhn.no:3000/api"
    test_int: "69270392b5a5a50bd8b64de6" # Only used for development testing
  splunk:
    url: "https://splunk-hec.nhn.no"
    index: "dc_nam"
    source: "ror-firewall-ssi:test"

credentials:
  namToken: "{{ .Values.credentials.namToken }}"
  splunkToken: "{{ .Values.credentials.splunkToken }}"

workspace: ssi